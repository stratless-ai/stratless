{
  "name": "stratless",
  "private": true,
  "description": "stratless — the local, private person-layer profiler",
  "scripts": {
    "test": "pnpm --filter ./cli test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=18" }
}
