{
  "name": "stratless",
  "private": true,
  "description": "stratless — the person-layer profiler (cli/) and stratless.com (web/)",
  "scripts": {
    "test": "pnpm --filter ./cli test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter ./web dev",
    "generate": "pnpm --filter ./web generate"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=18" }
}
