# Why this file exists

Claude Code loads `CLAUDE.md`, not `AGENTS.md`. This file exists only to pull the real guide into
context at session start. `AGENTS.md` is the source of truth — it's what Cursor, Aider, and the
product's own thesis point at. **Edit `AGENTS.md`, not this file.**

@AGENTS.md
